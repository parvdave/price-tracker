# Kafka producer
confluent-kafka==2.4.0
websockets==12.0

# PySpark (Spark 3.5.x)
pyspark==3.5.1

# FastAPI + server
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1

# Async PostgreSQL / TimescaleDB
asyncpg==0.29.0

# JDBC driver is supplied at spark-submit time via --packages; no pip entry needed.
